pokedex:
  external-apis:
    pokeapi:
      url-pattern: http://localhost:${wiremock.server.port:0}/pokeapi/{pokemonName}
    funtranslations:
      url-pattern: http://localhost:${wiremock.server.port:0}/funtranslations/{language}.json?text={text}
